for @a <- @{one,two,three}
for @b <- @{@a:$1,2,@3}
block auto !math
  for @c <- @{
      @a,
      @b
    }

  for @d <- @{@c:
      @{},
      @{@b:}
    }

  block !auto start
    block
      for @d <- notanarray

for @i <- @{}
